command -v nvim >/dev/null && vim="nvim" vimdiff="nvim -d"

# shortcuts
alias \
	v='$EDITOR' \
    yt='youtube-dl' \
	music='ncmpcpp' \
	py='python'

# flags
alias \
    r=". ranger" \
	ls='ls --color=auto -a' \
	grep='grep --color=auto' \
	du='du -h' \
	cp='cp -iv' \
	mv='mv -iv' \
	rm='rm -v' \
	mkdir='mkdir -pv' \
	make='make -j$(nproc)' \
	tarc='tar -czvf' \
	tarx='tar -xzvf' \
	ffmpeg='ffmpeg -hide_banner' \

# long
alias \
	7zs='7z a -t7z -mx=9 -mfb=273 -ms -md=31 -myx=9 -mtm=- -mmt -mmtf -md=1536m -mmf=bt3 -mmc=10000 -mpb=0 -mlc=0' \
	cl="curl -F 'clbin=<-' https://clbin.com" \
	insta='instaloader --stories --highlights --igtv --fast-update' \
	uvr='py /media/storage/docs/archive/tools/UVR-V4GUI/VocalRemover.py' \
	gp='git add . && git commit -m "$(date +%F_%N)" && git push -u origin master'
